/dist
/node_modules
/build
# Keep environment variables out of version control
.env

/generated/prisma

# Ignore uploaded files but keep the directory (if a .gitkeep is added later)
/uploads/*
!/uploads/.gitkeep
